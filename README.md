# 🕵️ The eye of God — Video Retrieval (VLM + фильтры)

Локальное приложение для **поиска “интересных” моментов в видео** и **ранжирования фрагментов по текстовому запросу** (*text → video retrieval*).

Проект включает:
- **Web UI (Flask):** загрузка видео → выбор ROI → обработка → список **TOP‑20 таймкодов** для быстрого перехода.
- **CLI batch:** ранжирование **уже подготовленных сегментов** (или сегментов из `segments_manifest.json`) по одному запросу.

---

## ✨ Возможности

- ✅ Разбиение видео на сегменты фиксированной длины (`SEGMENT_SECONDS`)
- ✅ ROI (область интереса) — обрезка сегментов по прямоугольнику
- ✅ Motion filter — оставляет сегменты с движением длительностью ≥ `MIN_MOTION_SECONDS`
- ✅ Person filter — оставляет сегменты с человеком (YOLO, COCO class=0)
- ✅ VLM retrieval: CLIP‑based / XCLIP‑based backends
- ✅ Кэширование индекса и результатов (`./cache/`)
- ✅ RU→EN перевод запросов (если запрос на кириллице)

---

## 📦 Структура проекта

```
.
├── main.py                # Web (Flask) приложение + пайплайн
├── batch_run.py           # CLI batch: ранжирование сегментов/манифестов
├── run_one_video.py       # core: index + retrieve + export (используется web)
├── index_video.py         # индексация клипов (эмбеддинги)
├── Utils.py               # split/motion/person/translate + retrieval helpers
├── cache_io.py            # кэш: index/results/meta
├── config.py              # конфиг + CLI аргументы
├── models/                # video-text backends
│   ├── base.py
│   ├── registry.py
│   ├── clip_frame_backend.py
│   ├── xclip_backend.py
│   └── dummy_backend.py
├── templates/             # HTML шаблоны Flask
├── static/                # фронт (JS/CSS/иконка)
└── yolo11s.pt             # веса YOLO (используются person_detect)
```

---

## ⚙️ Установка

### 1) Python окружение
Рекомендуется **Python 3.9+**.

```bash
python -m venv .venv
# Linux/Mac:
source .venv/bin/activate
# Windows:
.venv\Scripts\activate
```

### 2) Зависимости
```bash
pip install -r requirements.txt
```

**Примечания:**
- **PyTorch:** если есть CUDA — ставьте PyTorch под вашу версию CUDA.
- **Hugging Face модели** (CLIP/XCLIP и переводчик) могут скачиваться при первом запуске (нужен интернет).
- Для некоторых функций экспорта (CLI) может понадобиться **ffmpeg** в `PATH` (см. ниже).

---

## 🚀 Быстрый старт (Web UI)

Запуск локального приложения:

```bash
python main.py
```

- Приложение само откроет браузер.
- Загрузите видео, введите текстовый запрос.
- На следующем шаге выберите **ROI**.
- Дождитесь обработки → получите **TOP‑20 таймкодов** (кликабельные, с переходом по видео).

### Что делает web‑пайплайн
1) `split_video_web(...)` — режет исходное видео на сегменты и **кропает по ROI**  
2) `motion_detect(...)` → `person_detect(...)` — фильтрует сегменты  
3) Создаёт `segments_manifest.json` в temp‑директории задачи  
4) `process_one_video(...)` — считает эмбеддинги и ранжирует сегменты по запросу

> Важно: в web‑режиме детекторы **не получают roi напрямую**, потому что сегменты уже кропнуты на этапе split.

---

## 🧩 Конфигурация (Web)

В web‑режиме основные параметры пайплайна задаются константами в `main.py`:

```py
SEGMENT_SECONDS = 5.0
MIN_MOTION_SECONDS = 3.0
PERSON_N_FRAMES = 8
```

Модель/устройство задаются дефолтами в `config.py` (`Config.model`), например:
- `backend`: `clip_frame` или `xclip`
- `device`: `cpu` или `cuda:0`
- `dtype`: `fp32` / `fp16` (обычно `fp16` имеет смысл на CUDA)

---

## 🖥️ CLI Batch (ранжирование готовых сегментов)

Batch‑режим предназначен для папки, в которой есть:
- либо **готовые сегменты** (`.mp4/.mkv/...`),
- либо подпапки/файлы с `segments_manifest.json` (формат ниже).

Запуск:

```bash
python batch_run.py --mode batch --video /path/to/segments_or_manifests --query "Vandalism"
```

Результаты по умолчанию пишутся в `./batch_out/`:
- `summary.json` — агрегированный список лучших сегментов
- `report.json` — подробный отчёт (времена, метрики, ошибки)

### Формат `segments_manifest.json`
Web‑режим создаёт манифест примерно такого вида:

```json
{
  "source_video": "/abs/path/to/video.mp4",
  "segments_dir": "/abs/path/to/segments_dir",
  "segment_seconds": 5.0,
  "use_roi": true,
  "roi": [x, y, w, h],
  "segments": [
    {"path": "/abs/path/to/seg_00000.mp4", "start_time_sec": 0.0},
    {"path": "/abs/path/to/seg_00001.mp4", "start_time_sec": 5.0}
  ]
}
```

Если вы сами готовите сегменты — достаточно обеспечить:
- `segments[].path`
- `segments[].start_time_sec` (для корректных таймкодов)

---

## 🧠 Backends (модели)

Доступные backends зарегистрированы в `models/`:

- **`clip_frame`** — быстрее (особенно на CPU), но хуже понимает динамику.
- **`xclip`** — лучше по качеству на “действиях”, но тяжелее.

Переключение (в CLI):
```bash
python batch_run.py --mode batch --video ... --query "Person running" --backend xclip --device cuda:0 --dtype fp16
```

> В web‑режиме переключение делается через дефолты в `config.py` или правкой `main.py`.

---

## 🗂️ Кэш

Кэш хранится в `./cache/` и включает:
- индексы эмбеддингов (PyTorch `.pt`)
- результаты retrieval (`.json`)
- метаданные запусков

В web‑режиме кэш располагается рядом с запускаемым файлом/`exe` (логика `runtime_dir()` в `main.py`).

---

## 🎞️ Экспорт клипов (опционально, CLI)

В `run_one_video.py` есть экспорт TOP‑K сегментов через **ffmpeg** (если включить `export_clips` в конфиге/аргументах).

Если экспорт нужен:
1) установите ffmpeg
2) убедитесь, что `ffmpeg` и `ffprobe` доступны из командной строки

